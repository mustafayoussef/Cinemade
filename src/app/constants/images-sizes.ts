import { environment } from './../../environments/environment';

export const IMAGES_SIZES = {
  small: `${environment.imageUrl}/w185`,
  medium: `${environment.imageUrl}/w342`,
  large: `${environment.imageUrl}/original`
};
